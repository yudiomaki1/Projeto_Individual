<div class="sidebar">
        <div>
            <div class="sidebar-header">Taskly</div>
            <div class="sidebar-menu">
                <a href="/home" class="sidebar-menu-btn">
                    <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                        <rect x="3" y="4" width="18" height="18" rx="2"/>
                        <path d="M16 2v4M8 2v4M3 10h18"/>
                    </svg>
                    Calendário
                </a>
                <a href="/notes" class="sidebar-menu-btn">
                    <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                        <path d="M4 19.5V5a2 2 0 0 1 2-2h8a2 2 0 0 1 2 2v14.5"/>
                        <path d="M9 17h6"/>
                        <path d="M9 13h6"/>
                        <path d="M9 9h6"/>
                    </svg>
                    Anotações
                </a>

                <a href="/createTask" class="sidebar-menu-btn">
                    Adicionar Anotações
                </a>
            </div>
        </div>
        <div class="sidebar-footer">
            <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <circle cx="12" cy="8" r="4"/>
                <path d="M6 20v-2a4 4 0 0 1 4-4h4a4 4 0 0 1 4 4v2"/>
            </svg>
            <span>Usuário</span>
        </div>
    </div>
    <script>
    fetch('/api/user')
        .then(response => response.json())
        .then(data => {
            const userSpan = document.querySelector('.sidebar-footer span');
            userSpan.textContent = data.name || 'Usuário';
        })
        .catch(error => console.error('Erro ao carregar usuário:', error));
</script>